# 段2 の判定で「説明の付く差」として宣言するもの。
#
# ⚠ 突き合わせ先は **当部がいま配っている** jar のほう:
#     worlds/world-3/dev/instance/mods/origins-forge-1.20.1-1.10.0.9-all-eruto1.jar
#
# ⚠⚠ **なぜ配布された素の jar ではないのか**: 段2 の狙いは
#   「jar の中の class を差し替える手術を、ビルドで再現できたか」なので、
#   ⚠ **手術済みの物と突き合わせないと判定にならない**。
#
#     py -3.12 tools/compare_with_released.py \
#         --built origins/build/libs/origins-forge-1.20.1-1.10.0.9-all.jar \
#         --released <当部の -eruto1.jar> \
#         --allow tools/expected-diffs-stage2.txt
#
# ⚠ 1行1つ。`#` から後ろは理由（**必ず書く**）。
# ⚠ **当たらなかった宣言が在ると落ちる**——期待していた差が消えたということなので、
#    宣言が古いか、当部の変更が落ちたかのどちらか。放置すると検査が空回りする。

# ⚠⚠ **理由が事実かは誰も見ていない**（2026-09-01 に痛い目を見た）。
#    この道具が見るのは「宣言が当たったか」だけで、⚠ **`#` から後ろは検査されない。**
#    ⚠ 実際、下の2件は**2件とも理由が事実と違い**、そのうち1件が
#    ⚠⚠ **依頼者のクライアントを起動時に落とした**（refmap が12件欠けていた）。
#    ⚠ **宣言を足すときは、その理由を1つ機械で確かめてから書く。**

io/github/apace100/apoli/mixin/forge/EarsCompatMixin.class # ⚠ 2026-09-01 訂正: 前は「配っている jar には入っていない」と書いていたが**誤り**（実際は 2,892 バイトで入っている）。正しくは「配っている版は**素のまま**で、当部はソースだけ直した」（08bc5ba の本文が正しい）。ソースから建てると当部の版になるので中身が違う

apoli.refmap.json # ⚠ 2026-09-01 訂正: 前は「並びが変わるだけ」と書いていたが**誤り**——**12件が欠けていた**のを、この宣言が握りつぶしていた（`annotationProcessor` に mixinextras が載っていなかった）。⚠ **中身は `py -3.12 tools/check_refmap_coverage.py` が別に見る**（ソースの注釈が要求する参照が全部載っているか。陰性対照＝配っている jar は欠け 0）。⚠ **ここで許すのは並びと書式の差だけ**——その前提はあちらの検査が支えている
