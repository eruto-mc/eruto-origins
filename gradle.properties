org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

calio_specs=1.11.0
apoli_specs=2.9.0
origins_specs=1.10.0

calio_version=1.11.0.5
apoli_version=2.9.0.8
origins_version=1.10.0.9

# ⚠⚠ **当部が足した（2026-09-08）。`origins-classes` をここのサブプロジェクトにするため。**
# ⚠ 版は配っている jar の実値（`Implementation-Version: 1.2.1` ／ `Specification-Version: 1`）。
# ⚠ 依存の版は `origins-classes/gradle.properties` から写した。
origins-classes_specs=1
origins-classes_version=1.2.1

farmersdelight_file=4679319
appleskin_file=4770828
placebo_file=4913663
apotheosis_file=4874713
attlib_file=4872467
mutil_file=4824501
tetra_file=4857842

minecraft_version=1.20.1
forge_version=47.1.0
mixin_version=0.8.5

mappings_channel=parchment
mappings_version=2023.09.03-1.20.1

aea_version=1.4.0.5
jei_version=15.2.0.27
caelus_version=3.1.0
citadel_file=4726879
ears_file=4452800

forge_requirements=[47.1,)

# ⚠⚠ **当部が足した（2026-09-08）。`origins-classes` の `mods.toml` が使う。**
# ⚠ あちらは `${mc_requirements}` と `${origins_requirements}` も展開するが、
#   ルートの `processResources` は `version` / `mc_version` / `forge_requirements` しか渡していなかった。
# ⚠ 値は `origins-classes/gradle.properties`（消した `build.gradle` の相棒）から写した。
mc_requirements=[1.20.1,)
origins_requirements=[1.20.1-1.10.0.2,)
